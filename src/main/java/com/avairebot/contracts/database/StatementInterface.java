package com.avairebot.contracts.database;

public interface StatementInterface {
    // This interface is empty, it's only used for a hierarchy lookup
    // when comparing types, allowing us to use and store more
    // than one type of statement under the same object.
}
