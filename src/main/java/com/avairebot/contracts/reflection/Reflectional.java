package com.avairebot.contracts.reflection;

public interface Reflectional {
    // This interface is empty, it is only used to for auto loading classes,
    // any class that implements this interface must also create a
    // constructor that takes in one argument which is the AvaIre
    // instance, look at Reflectionable for reference.
}
