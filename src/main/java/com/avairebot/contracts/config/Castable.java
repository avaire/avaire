package com.avairebot.contracts.config;

public interface Castable {
    // This interface is empty, it's only use is to allow Java to cast configuration
    // implementations from the castable interface which is returned from the
    // configuration loaded into their actually class instances.
}
